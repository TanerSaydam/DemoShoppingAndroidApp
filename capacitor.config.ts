import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.demo',
  appName: 'DemoShoppingAndroidApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
